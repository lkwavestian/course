//场地管理
import React, { PureComponent } from 'react';
import ReplaceApplication from '../../components/ReplaceApplication';

export default class ReplaceApplicationRouteIndex extends PureComponent {
    constructor(props) {
        super(props);
    }

    render() {
        return <ReplaceApplication />;
    }
}
